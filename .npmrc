# Required for `pnpm deploy` (used in apps/api/Dockerfile) to produce a
# self-contained node_modules: workspace deps like @quiz/shared are copied in
# rather than symlinked, so the deployed package works outside the monorepo.
inject-workspace-packages=true
